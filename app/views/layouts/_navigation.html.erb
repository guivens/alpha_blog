<nav class="navbar navbar-expand-lg bg-primary " >
    <div class="container-fluid ">
      <%= link_to "ALPHA BLOG", root_path, class: "navbar-brand" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "Bloggers", users_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "About", about_path, class: "nav-link"%>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Articles
            </a>
            <ul class="dropdown-menu bg-primary">
              <%= link_to "Articles listing", articles_path, class: "dropdown-item" %>
              <% if logged_in? %>
              <%= link_to "create article", new_article_path, class: "dropdown-item" %>
              <% end %>
            </ul>
          </li>
        </ul>

          <ul class="navbar-nav d-flex justify-content-end">
            <% if logged_in? %>

            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <%= "(Admin)" if current_user.admin? %> Profile <%= current_user.username %>
              </a>
              <ul class="dropdown-menu bg-primary">
                <%= link_to "View profile", user_path(current_user), class: "dropdown-item" %>
                <%= link_to "Edit profile", edit_user_path(current_user), class: "dropdown-item" %>
                <li><hr class="dropdown-divider"></li>
                <%= link_to "Delete profile", user_path(current_user), data: {"turbo-method": :delete, confirm: "are you sure"}, class: "dropdown-item text-danger" %>
              </ul>
            </li>

              <li class="nav-item">
                <%= link_to 'Log out', logout_path, data: {"turbo-method": :delete}, class: "nav-link" %>
              </li>
              <% else %>
              <li class="nav-item">
                <%= link_to 'Log in', login_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to 'sign up', signup_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
       <!-- <form class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-light" type="submit">Search</button>
        </form>-->
      </div>
    </div>
  </nav>